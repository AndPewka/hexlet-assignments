= simple_form_for @post do |f|
  = f.input :title, label: false, placeholder: t('simple_form.placeholders.post.title')
  = f.input :body, placeholder: t('simple_form.placeholders.post.body')
  = f.submit class: 'btn btn-success', value: t('.title')
